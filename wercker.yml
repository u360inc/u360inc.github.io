box: michilu/deploy-gh-pages
build:
  steps:
    - bundle-install:
        binstubs: true
        jobs: 4
    - script:
        name: build
        code: make
  after-steps:
    - hipchat-notify:
        token: $HIPCHAT_TOKEN
        room-id: $HIPCHAT_ID
deploy:
  steps:
    - bundle-install:
        binstubs: true
        jobs: 4
    - script:
        name: build
        code: make
    # https://github.com/lvivier/step-gh-pages
    - lukevivier/gh-pages:
      domain: u360inc.com
      repo: u360inc/u360inc.github.io
      token: $GITHUB_TOKEN
  after-steps:
    - hipchat-notify:
        token: $HIPCHAT_TOKEN
        room-id: $HIPCHAT_ID
