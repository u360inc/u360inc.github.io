box: michilu/deploy-gh-pages
build:
  steps:
    - bundle-install:
        binstubs: true
        jobs: 4
    - script:
        name: build
        code: make
deploy:
  steps:
    # https://github.com/lvivier/step-gh-pages
    - lukevivier/gh-pages:
      domain: u360inc.com
      repo: u360inc/u360inc.github.io
      token: $GITHUB_TOKEN
